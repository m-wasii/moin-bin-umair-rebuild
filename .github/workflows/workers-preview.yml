name: Worker preview

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

concurrency:
  group: worker-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  preview:
    name: Deploy preview Worker
    if: >
      github.event.action != 'closed' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Require Cloudflare secrets
        run: |
          if [ -z "${CLOUDFLARE_API_TOKEN:-}" ] || [ -z "${CLOUDFLARE_ACCOUNT_ID:-}" ]; then
            echo "::error::Add repository secrets CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID at https://github.com/${GITHUB_REPOSITORY}/settings/secrets/actions then re-run this workflow."
            exit 1
          fi
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Deploy preview Worker
        id: deploy
        run: |
          set -euo pipefail
          name="mbu-pr-${{ github.event.pull_request.number }}"
          npx wrangler deploy --name "$name" \
            --message "PR ${{ github.event.pull_request.number }} @ ${GITHUB_SHA:0:7}" \
            | tee wrangler-preview.log
          url="$(sed -n 's/^[[:space:]]*https:\/\/.*\.workers\.dev.*/&/p' wrangler-preview.log | tr -d '[:space:]' | tail -n 1)"
          if [ -z "$url" ]; then
            url="https://${name}.wasi-workdesk.workers.dev"
          fi
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "name=$name" >> "$GITHUB_OUTPUT"
      - name: Comment preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const marker = "<!-- workers-preview -->";
            const url = process.env.PREVIEW_URL;
            const body = [
              marker,
              "## Worker preview",
              "",
              url,
              "",
              "This is a dedicated Worker (`mbu-pr-${{ github.event.pull_request.number }}`), not Pages. Production `mbu` is unchanged.",
            ].join("\n");
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find((comment) =>
              comment.body?.includes(marker),
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
              return;
            }
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });
        env:
          PREVIEW_URL: ${{ steps.deploy.outputs.url }}

  cleanup:
    name: Delete preview Worker
    if: >
      github.event.action == 'closed' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Require Cloudflare secrets
        run: |
          if [ -z "${CLOUDFLARE_API_TOKEN:-}" ] || [ -z "${CLOUDFLARE_ACCOUNT_ID:-}" ]; then
            echo "::error::Add repository secrets CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID at https://github.com/${GITHUB_REPOSITORY}/settings/secrets/actions"
            exit 1
          fi
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - name: Delete preview Worker
        run: |
          npx wrangler delete --name "mbu-pr-${{ github.event.pull_request.number }}" --force
        continue-on-error: true
